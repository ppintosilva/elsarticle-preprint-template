\documentclass[$if(elsarticle)$$elsarticle$$endif$]{elsarticle}

\usepackage{hyperref}
$if(geometry)$
\usepackage[$geometry$]{geometry}
$endif$

$if(line-numbers)$
\usepackage{lineno}
\modulolinenumbers[$line-numbers$]
$endif$

$if(journal)$
\journal{$journal$}
$endif$

$for(header-includes)$
$header-includes$
$endfor$

$if(tightlist)$
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
$else$
\def\tightlist{}
$endif$

$if(indent)$
$else$
\makeatletter
\setlength{\parindent}{0pt}
\makeatother
$endif$

$if(raggedbottom)$
\raggedbottom
$endif$

$if(bibstyle)$
\bibliographystyle{$bibstyle$}
$else$
\bibliographystyle{bst/harvard}
\biboptions{authoryear}
$endif$

$if(biboptions)$
\biboptions{$biboptions$}
$endif$

\begin{document}

\begin{frontmatter}

$frontmatter$

\end{frontmatter}

$if(line-numbers)$
\linenumbers
$endif$

$body$

$if(bibfile)$
\bibliography{$bibfile$}
$else$
\bibliography{references}
$endif$

\end{document}